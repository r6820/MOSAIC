
// const port = '5000';
// const server = `http://localhost:${port}`;

const server = 'https://mosaic-server.onrender.com';

export const SOCKET_URL = `${server}/api/io`;
export const API_URL = `${server}/api`